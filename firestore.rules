
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to the games collection
    // This is for demonstration purposes and should be secured for a real application
    match /games/{gameId} {
      allow read, write: if true;
    }
  }
}
