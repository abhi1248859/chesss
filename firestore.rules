
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read and write to the games collection.
    // This is useful for anonymous/guest users.
    match /games/{gameId} {
      allow read, write: if true;
    }
  }
}
